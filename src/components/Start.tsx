import "../styles/Start.css"

export default function Start() {
    return (
        <div className="startContainer">
            <div className="textContainer" >
                <h1 className="startHeader">startheader stuff idskkfsdiojsdfjoik</h1>
                <p className="startDesc" >start description ukukukstart description ukukukstart description ukukukstart description ukukukstart description ukukukstart description ukukuk</p>
            </div>
        </div>
    )
}